{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python DBG: RUN",
            "type": "debugpy",
            "request": "launch",
            "cwd": "${workspaceFolder}/src",
            "module": "knot_wrapper.example"
        }
    ]
}